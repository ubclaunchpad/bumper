import React from 'react';

export default class Player extends React.Component {
  constructor(mass, position, velocity, color, name) { 
    this.mass = mass;
	this.position = position;
	this.velocity = velocity;
	this.color = color;
	this.name = name;
  }

  move() {
	  
  }
  
}